# Alpha 40 — Three Artifacts

1. alpha40-companion.html — The Tome (flipbook), Deckbuilder (enforces all groups), Arena (playtest)
2. mtg-alpha-40-v3-images.html — Y2K Layers engine
3. download_alpha_offline.py — run `py download_alpha_offline.py` to embed Scryfall images

## Quick start
1. Unzip into C:\Users\Dave\FractalKernel\
2. Double-click alpha40-companion.html
3. git add . && git commit -m "Alpha 40" && git push
